# --- CORE ---
bind = SUPER, Return, exec, $terminal # Terminal
bind = SUPER, Q, killactive, # Chiudi finestra
bind = SUPER, M, exec, hyprshutdown || hyprctl dispatch exit # Spegnimento
bind = SUPER, E, exec, $fileManager # File manager
bind = SUPER, Space, exec, walker # App launcher

# --- BROWSER ---
bind = SUPER, B, exec, ~/.config/hypr/script/launch-browser.sh # Browser
bind = SUPER SHIFT, B, exec, ~/.config/hypr/script/launch-browser.sh --private # Browser privato

# --- WINDOW ---
bind = SUPER, P, pseudo, # Pseudo tiling
bind = SUPER, J, togglesplit, # Toggle split

# --- SYSTEM ---
bind = SUPER, L, exec, hyprlock # Lock schermo
bind = SUPER, K, exec, ~/.config/hypr/script/keybinds.sh # Keybinds menu

# --- WALLPAPER ---
bind = SUPER, W, exec, waypaper --folder ~/Pictures/wallpapers # Selettore wallpaper

# whatsapp
bind = SUPER SHIFT, W, exec, elecwhat # Whatsapp

# --- CLIPBOARD ---
bind = SUPER, V, exec, ~/.config/hypr/script/clipboard.sh # Clipboard manager

# --- PACKAGE ---
bind = SUPER, P, exec, kitty --class "pkg-manager" -e $HOME/.config/hypr/script/installer.sh # Package manager
windowrule = float 1, match:class pkg-manager
windowrule = size 1700 900, match:class pkg-manager
windowrule = center 1, match:class pkg-manager

bind = SUPER, R, exec, kitty --class "upkg-manager" -e $HOME/.config/hypr/script/uninstall.sh # Package manager
windowrule = float 1, match:class upkg-manager
windowrule = size 1700 900, match:class upkg-manager
windowrule = center 1, match:class upkg-manager



# --- DEV ---
bind = SUPER SHIFT, V, exec, code # VSCode

# --- FOCUS ---
bind = SUPER, left, movefocus, l # Focus sinistra
bind = SUPER, right, movefocus, r # Focus destra
bind = SUPER, up, movefocus, u # Focus su
bind = SUPER, down, movefocus, d # Focus giù

# --- WORKSPACE ---
bind = SUPER, 1, workspace, 1 # Workspace 1
bind = SUPER, 2, workspace, 2 # Workspace 2
bind = SUPER, 3, workspace, 3 # Workspace 3
bind = SUPER, 4, workspace, 4 # Workspace 4
bind = SUPER, 5, workspace, 5 # Workspace 5
bind = SUPER, 6, workspace, 6 # Workspace 6
bind = SUPER, 7, workspace, 7 # Workspace 7
bind = SUPER, 8, workspace, 8 # Workspace 8
bind = SUPER, 9, workspace, 9 # Workspace 9
bind = SUPER, 0, workspace, 10 # Workspace 10

# --- MOVE WORKSPACE ---
bind = SUPER SHIFT, 1, movetoworkspace, 1 # Sposta workspace 1
bind = SUPER SHIFT, 2, movetoworkspace, 2 # Sposta workspace 2
bind = SUPER SHIFT, 3, movetoworkspace, 3
bind = SUPER SHIFT, 4, movetoworkspace, 4
bind = SUPER SHIFT, 5, movetoworkspace, 5
bind = SUPER SHIFT, 6, movetoworkspace, 6
bind = SUPER SHIFT, 7, movetoworkspace, 7
bind = SUPER SHIFT, 8, movetoworkspace, 8
bind = SUPER SHIFT, 9, movetoworkspace, 9
bind = SUPER SHIFT, 0, movetoworkspace, 10

# --- SPECIAL ---
bind = SUPER, S, togglespecialworkspace, magic # Toggle scratchpad
bind = SUPER SHIFT, S, movetoworkspace, special:magic # Sposta scratchpad

# --- MOUSE ---
bindm = SUPER, mouse:272, movewindow # Muovi finestra
bindm = SUPER, mouse:273, resizewindow # Ridimensiona finestra

# --- AUDIO ---
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+ # Volume su
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%- # Volume giù
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle # Mute
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle # Mic mute

# --- BRIGHTNESS ---
bindel = ,XF86MonBrightnessUp, exec, brightnessctl set 5%+ # Luminosità su
bindel = ,XF86MonBrightnessDown, exec, brightnessctl set 5%- # Luminosità giù

# --- SCREENSHOT ---
bind = SUPER, Print, exec, ~/.config/hypr/script/screenshot.sh # Screenshot
bind = CTRL, Print, exec, grim -g "$(slurp)" - | wl-copy # Screenshot clipboard


windowrule = float 1, match:class screenshot
windowrule = size 1400 900, match:class screenshot
windowrule = center 1, match:class screenshot

# --- MEDIA ---
bindl = , XF86AudioNext, exec, playerctl next # Traccia successiva
bindl = , XF86AudioPause, exec, playerctl play-pause # Play/Pausa
bindl = , XF86AudioPlay, exec, playerctl play-pause # Play
bindl = , XF86AudioPrev, exec, playerctl previous # Traccia precedente
